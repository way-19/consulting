{
  "name": "consulting19-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup:env": "node scripts/setupEnv.js",
    "dev": "npm run dev:marketing",
    "dev:marketing": "npm run setup:env && npm run dev --workspace=apps/marketing",
    "dev:client": "npm run setup:env && cd apps/dashboard && npm run dev:client",
    "dev:consultant": "npm run setup:env && cd apps/dashboard && npm run dev:consultant",
    "dev:admin": "npm run setup:env && cd apps/dashboard && npm run dev:admin",
    "build": "npm run build --workspace=apps/marketing"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "react-helmet-async": "^2.0.5"
  }
}
