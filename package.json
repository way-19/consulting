{
  "name": "consulting19-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup:env": "node scripts/setupEnv.js",
    "dev": "npm run setup:env && npm run dev --workspace=apps/marketing",
    "dev:marketing": "npm run setup:env && npm run dev --workspace=apps/marketing",
    "dev:dashboard": "npm run setup:env && npm run dev --workspace=apps/dashboard",
    "build": "npm run build --workspace=apps/marketing"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "react-helmet-async": "^2.0.5"
  }
}
